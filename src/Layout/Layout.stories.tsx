import React from 'react';
import Layout from './Layout';
import Base from '../Base/Base';

export default {
  title: 'Layout',
  component: Layout,
};

export const Basic = () => (
  <Base>
    <Layout />
  </Base>
);
